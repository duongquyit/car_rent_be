const host: string = '';
const port: number = 0;
const username: string = '';
const password: string = '';
const database: string = '';

export const configMigration = {
  host,
  port,
  username,
  password,
  database,
};
